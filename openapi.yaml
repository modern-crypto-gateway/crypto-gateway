openapi: 3.1.0
info:
  title: crypto-gateway
  version: 0.0.0
  description: |
    Runtime-agnostic crypto payment gateway with pluggable chain adapters
    (EVM / Tron / Solana), DB adapters (D1 / libSQL / Postgres), cache
    adapters, detection strategies, and webhook delivery.

    Deploys on Cloudflare Workers, Node/Bun, Deno, and Vercel Edge from one
    codebase.

    ### Deployment knobs (not HTTP routes)

    The behaviors below are server-side config read at boot and do not
    surface as endpoints. They're documented here so operators who only
    have the OpenAPI spec can discover them.

    **Price oracle fallback chain.** Invoice quoting walks a chained list
    of providers on every request; each link tries itself first and
    delegates to the next on error/timeout/unmapped symbol. Only the
    terminal `static-peg` link serves hardcoded values. Default ordering
    when `ALCHEMY_API_KEY` is set:
    `CoinGecko → Alchemy → CoinCap → Binance → static-peg`.
    Knobs: `PRICE_ADAPTER=alchemy|coingecko|static-peg` (swaps the primary
    or collapses to the static link), `COINGECKO_API_KEY` +
    `COINGECKO_PLAN=demo|pro`, `COINCAP_API_KEY`, and per-provider
    opt-outs `DISABLE_COINGECKO=1` / `DISABLE_COINCAP=1` /
    `DISABLE_BINANCE=1` / `DISABLE_ALCHEMY=1`. `PRICE_ADAPTER=alchemy`
    without a key logs a warning and falls back to the default ordering.

    **Log shipping + alerting.** Setting `ALERT_WEBHOOK_URL` attaches an
    HTTP sink to the structured logger; every `error` or `fatal` log is
    POSTed as `{ ts, level, msg, ...fields }` JSON. Sliding-window rate
    limited to 30/min with a `droppedSinceLast` counter piggybacked on
    the next successful delivery. Use `ALERT_WEBHOOK_AUTH_HEADER` for the
    `Authorization` header value (e.g. `Bearer xyz`). Fire-and-forget —
    a broken sink never breaks the caller's log path.

    See the repository README for the full env-var table.

servers:
  - url: http://localhost:8787
    description: Local Node dev
  - url: https://crypto-gateway-dev.workers.dev
    description: Dev Workers (placeholder — replace with your subdomain)

tags:
  - name: invoices
    description: Merchant-facing invoice lifecycle
  - name: payouts
    description: Merchant-facing payout lifecycle
  - name: checkout
    description: Public invoice view (for hosted checkout UIs)
  - name: webhooks
    description: Inbound webhook ingest from on-chain providers
  - name: admin
    description: Operator-only setup
  - name: internal
    description: Scheduler-driven maintenance
  - name: health

security:
  - MerchantApiKey: []

paths:
  /health:
    get:
      tags: [health]
      summary: Liveness probe
      security: []
      responses:
        "200":
          description: Service up
          content:
            application/json:
              schema:
                type: object
                required: [status, phase]
                properties:
                  status: { type: string, enum: [ok] }
                  phase: { type: integer }

  /api/v1/invoices:
    get:
      tags: [invoices]
      summary: List invoices for the authenticated merchant
      description: |
        Returns invoices created by the authenticated merchant, newest first
        (`createdAt DESC`). All filters are optional and AND-combined. The
        `merchantId` is always taken from the API key — there is no way to
        list another merchant's invoices.

        Pagination uses `limit` + `offset`. The response includes `hasMore`
        (computed without a `COUNT(*)`), so callers can page until `hasMore`
        is false.
      parameters:
        - name: status
          in: query
          description: |
            Comma-separated list of statuses. Any of:
            `created,partial,detected,confirmed,overpaid,expired,canceled`.
            Unknown values yield 400.
          schema: { type: string }
        - name: chainId
          in: query
          schema: { type: integer }
        - name: token
          in: query
          schema: { type: string }
        - name: externalId
          in: query
          description: Merchant-supplied dedup key (exact match).
          schema: { type: string }
        - name: toAddress
          in: query
          description: |
            Invoice receive address — what the payer saw on the checkout page.
            Exact match; pass the canonicalized form.
          schema: { type: string }
        - name: fromAddress
          in: query
          description: |
            Payer address. Matches invoices that have any detected or confirmed
            transaction from this address. Reverted / orphaned transactions
            are excluded so a single failed payment doesn't surface a stale
            invoice.
          schema: { type: string }
        - name: createdFrom
          in: query
          description: Inclusive lower bound on `createdAt`. ISO-8601 or unix-ms.
          schema: { type: string }
        - name: createdTo
          in: query
          description: Inclusive upper bound on `createdAt`. ISO-8601 or unix-ms.
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: offset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Paginated list of invoices
          content:
            application/json:
              schema:
                type: object
                required: [invoices, limit, offset, hasMore]
                properties:
                  invoices:
                    type: array
                    items: { $ref: "#/components/schemas/Invoice" }
                  limit: { type: integer }
                  offset: { type: integer }
                  hasMore: { type: boolean }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
    post:
      tags: [invoices]
      summary: Create an invoice
      description: |
        Creates an invoice and allocates a receive address derived from the
        gateway's HD seed. The merchant is resolved from the API key — the
        caller MUST NOT include `merchantId` in the body; if they do, it's
        ignored.

        **Idempotency.** Pass `externalId` to make the call idempotent: a
        retry with the same `(merchantId, externalId)` returns the existing
        invoice instead of creating a duplicate. Safe to send the same POST
        multiple times after a timeout or 5xx.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateInvoiceInput" }
      responses:
        "201":
          description: Invoice created
          content:
            application/json:
              schema:
                type: object
                required: [invoice]
                properties:
                  invoice: { $ref: "#/components/schemas/Invoice" }
          headers:
            X-Request-Id: { $ref: "#/components/headers/X-Request-Id" }
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/MerchantInactive" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /api/v1/invoices/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [invoices]
      summary: Fetch an invoice with USD breakdown and per-transaction detail
      description: |
        Returns the invoice along with two extra blocks not surfaced on POST
        responses:
          - `amounts` — USD-axis recomputation (required / confirmed /
            confirming / remaining / overpaid). All-null on legacy
            single-token invoices.
          - `transactions` — every on-chain row tied to this invoice (all
            statuses, including `reverted` and `orphaned` for debugging).
      responses:
        "200":
          description: Invoice found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InvoiceDetails" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/invoices/{id}/expire:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [invoices]
      summary: Force-expire an invoice before its expiry window elapses
      responses:
        "200":
          description: Invoice expired
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice: { $ref: "#/components/schemas/Invoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Invoice is in a terminal state and cannot be expired
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/payouts:
    get:
      tags: [payouts]
      summary: List payouts for the authenticated merchant
      description: |
        Returns payouts planned by the authenticated merchant, newest first
        (`createdAt DESC`). All filters optional, AND-combined. `merchantId`
        is always taken from the API key.

        Pagination via `limit` + `offset`; response includes `hasMore`
        (computed without `COUNT(*)`).
      parameters:
        - name: status
          in: query
          description: |
            Comma-separated list of statuses. Any of:
            `planned,reserved,submitted,confirmed,failed,canceled`.
          schema: { type: string }
        - name: chainId
          in: query
          schema: { type: integer }
        - name: token
          in: query
          schema: { type: string }
        - name: destinationAddress
          in: query
          description: Exact-match on the canonicalized destination.
          schema: { type: string }
        - name: sourceAddress
          in: query
          description: |
            Fee wallet that funded the payout. Populated only after reservation,
            so this filter implicitly excludes `planned` rows.
          schema: { type: string }
        - name: createdFrom
          in: query
          description: Inclusive lower bound on `createdAt`. ISO-8601 or unix-ms.
          schema: { type: string }
        - name: createdTo
          in: query
          description: Inclusive upper bound on `createdAt`. ISO-8601 or unix-ms.
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: offset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Paginated list of payouts
          content:
            application/json:
              schema:
                type: object
                required: [payouts, limit, offset, hasMore]
                properties:
                  payouts:
                    type: array
                    items: { $ref: "#/components/schemas/Payout" }
                  limit: { type: integer }
                  offset: { type: integer }
                  hasMore: { type: boolean }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }
    post:
      tags: [payouts]
      summary: Plan a payout
      description: |
        Creates a payout in `planned` state. A background job
        (`executeReservedPayouts`) reserves a fee wallet, signs, and
        broadcasts. Confirmation is surfaced via `payout.*` webhooks or a
        subsequent GET.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PlanPayoutInput" }
      responses:
        "201":
          description: Payout planned
          content:
            application/json:
              schema:
                type: object
                properties:
                  payout: { $ref: "#/components/schemas/Payout" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/MerchantInactive" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /api/v1/payouts/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [payouts]
      summary: Fetch a payout
      responses:
        "200":
          description: Payout found
          content:
            application/json:
              schema:
                type: object
                properties:
                  payout: { $ref: "#/components/schemas/Payout" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checkout/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [checkout]
      summary: Public invoice snapshot (for checkout UIs)
      description: |
        Narrow projection of an invoice suitable for end-user pages. The merchant
        identity and any merchant-private metadata are deliberately excluded.
        Rate limited per IP.
      security: []
      responses:
        "200":
          description: Invoice snapshot
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice: { $ref: "#/components/schemas/PublicInvoice" }
        "404": { $ref: "#/components/responses/NotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /webhooks/alchemy:
    post:
      tags: [webhooks]
      summary: Ingest Alchemy Notify ADDRESS_ACTIVITY webhooks
      description: |
        HMAC-SHA256 of the raw body under the per-webhook signing key
        (looked up by `event.webhookId` in the encrypted-at-rest
        `alchemy_webhook_registry` table) must match `X-Alchemy-Signature`.
        Returns 200 fast and processes the payload in the background;
        Alchemy retries on non-2xx.
      security:
        - AlchemySignature: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        "200":
          description: Acknowledged; ingest happens in background
          content:
            application/json:
              schema:
                type: object
                properties:
                  received: { type: boolean, const: true }
        "400": { description: Malformed JSON after a valid signature }
        "401": { description: Missing or bad signature }
        "404": { description: Alchemy ingest not enabled on this deployment }
        "429": { $ref: "#/components/responses/RateLimited" }

  /admin/merchants:
    get:
      tags: [admin]
      summary: List merchants
      description: |
        Paginated listing, newest first. Never leaks `api_key_hash` or
        `webhook_secret_ciphertext`. Use `?active=true|false` to scope to
        live or deactivated rows.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 500, default: 50 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
        - in: query
          name: active
          schema: { type: string, enum: [true, false] }
      responses:
        "200":
          description: Merchants page
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchants:
                    type: array
                    items: { $ref: "#/components/schemas/Merchant" }
                  limit: { type: integer }
                  offset: { type: integer }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }
    post:
      tags: [admin]
      summary: Create a merchant
      description: |
        Returns the plaintext API key ONCE in the response. The hash is
        stored; the plaintext is unrecoverable afterward.
      security:
        - AdminKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, minLength: 1, maxLength: 128 }
                webhookUrl: { type: string, format: uri }
                webhookSecret:
                  type: string
                  description: Optional hex-encoded 32-byte HMAC signing secret. Generated if omitted.
                  pattern: "^[0-9a-f]{64}$"
                paymentToleranceUnderBps:
                  type: integer
                  minimum: 0
                  maximum: 2000
                  default: 0
                  description: |
                    Default invoice under-payment tolerance in basis points.
                    A paid USD amount ≥ `amountUsd × (1 − bps/10_000)` closes
                    the invoice as `confirmed`. Example: `100` = 1 % — paying
                    99 USD on a 100 USD invoice still confirms.
                paymentToleranceOverBps:
                  type: integer
                  minimum: 0
                  maximum: 2000
                  default: 0
                  description: |
                    Default invoice over-payment tolerance in basis points.
                    A paid USD amount ≤ `amountUsd × (1 + bps/10_000)` closes
                    the invoice as `confirmed` (not `overpaid`). Example: `100`
                    = 1 % — paying 101 USD on a 100 USD invoice confirms.
                addressCooldownSeconds:
                  type: integer
                  minimum: 0
                  maximum: 604800
                  default: 0
                  description: |
                    Address-pool cooldown in seconds. After an invoice reaches a
                    terminal state, the pool address it held cannot be re-allocated
                    to a different invoice for this many seconds. Late payments
                    arriving during the cooldown still credit the original invoice
                    via the orphan + admin-attribute path. `0` (default) preserves
                    legacy immediate-reuse behavior. Capped at 7 days (604800s).
      responses:
        "201":
          description: Merchant created
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      webhookUrl: { type: [string, "null"] }
                      active: { type: boolean }
                      paymentToleranceUnderBps: { type: integer, minimum: 0, maximum: 2000 }
                      paymentToleranceOverBps: { type: integer, minimum: 0, maximum: 2000 }
                      addressCooldownSeconds: { type: integer, minimum: 0, maximum: 604800 }
                      createdAt: { type: string, format: date-time }
                  apiKey:
                    type: string
                    description: Plaintext API key. Shown once; store it immediately.
                    pattern: "^sk_[0-9a-f]{64}$"
                  webhookSecret:
                    type: string
                    description: Present only if `webhookUrl` was provided.
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }

  /admin/merchants/{id}:
    get:
      tags: [admin]
      summary: Get a single merchant
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Merchant
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant: { $ref: "#/components/schemas/Merchant" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }
    patch:
      tags: [admin]
      summary: Update a merchant
      description: |
        Mutates `name`, `webhookUrl`, and/or the three tuning defaults.
        `active` has its own endpoints (`/deactivate`, `/activate`); the API
        key rotates via `/rotate-key`; the webhook secret rotates via
        `/rotate-webhook-secret`.

        Special first-time `webhookUrl` case: if the merchant was created
        without a webhook URL and this PATCH sets one, the gateway mints a
        fresh HMAC secret, stores it encrypted, and returns the plaintext in
        `webhookSecret` — same one-shot contract as the create endpoint.
        Subsequent URL changes do NOT rotate the secret (signed-HMAC contract
        stays stable; the merchant just receives on a different URL).

        Tolerance changes apply to invoices created **after** the PATCH;
        existing invoices keep the snapshot they were issued with. Cooldown
        changes apply to releases that happen **after** the PATCH; already-
        stamped `cooldownUntil` deadlines are unaffected.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name: { type: string, minLength: 1, maxLength: 128 }
                webhookUrl: { type: string, format: uri }
                paymentToleranceUnderBps:
                  type: integer
                  minimum: 0
                  maximum: 2000
                paymentToleranceOverBps:
                  type: integer
                  minimum: 0
                  maximum: 2000
                addressCooldownSeconds:
                  type: integer
                  minimum: 0
                  maximum: 604800
      responses:
        "200":
          description: Merchant updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      webhookUrl: { type: [string, "null"] }
                      active: { type: boolean }
                      paymentToleranceUnderBps: { type: integer }
                      paymentToleranceOverBps: { type: integer }
                      addressCooldownSeconds: { type: integer }
                      updatedAt: { type: string, format: date-time }
                  webhookSecret:
                    type: string
                    description: Plaintext webhook secret; present ONLY when this PATCH set a `webhookUrl` on a merchant that didn't previously have one. Shown once.
                    pattern: "^[0-9a-f]{64}$"
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }

  /admin/merchants/{id}/rotate-key:
    post:
      tags: [admin]
      summary: Rotate a merchant's API key
      description: |
        Generates a fresh `sk_<hex>` plaintext and replaces the stored hash.
        The returned `apiKey` is the one and only chance to capture it. The
        prior key is rejected on the next request. Webhook URL/secret are
        intentionally not rotated here.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Key rotated; new plaintext returned once
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      active: { type: boolean }
                      updatedAt: { type: string, format: date-time }
                  apiKey:
                    type: string
                    description: Plaintext API key. Shown once; store it immediately.
                    pattern: "^sk_[0-9a-f]{64}$"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }

  /admin/merchants/{id}/rotate-webhook-secret:
    post:
      tags: [admin]
      summary: Rotate a merchant's webhook HMAC signing secret
      description: |
        Generates a fresh 32-byte hex plaintext, encrypts at rest, and
        returns the plaintext once so the operator can hand it to the
        merchant.

        **Coordinated-rotation caveat.** Outbox rows dispatched AFTER this
        call — including any queued before the rotation — are signed with
        the NEW secret (the dispatcher reads merchant state at send time).
        The merchant's verifier must hold the new secret when those arrive
        or they'll 401 and land in `webhook_deliveries.status='dead'`.
        Sequence it: announce → rotate → hand secret off → merchant updates.
        Failed retries can be replayed via
        `POST /admin/webhook-deliveries/{id}/replay` once the merchant
        confirms.

        Requires the merchant to have a `webhookUrl` configured. For
        first-time webhook setup, set the URL via `PATCH /admin/merchants/{id}`
        instead — that flow mints a fresh secret automatically.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Webhook secret rotated; new plaintext returned once
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      webhookUrl: { type: string, format: uri }
                      active: { type: boolean }
                      updatedAt: { type: string, format: date-time }
                  webhookSecret:
                    type: string
                    description: Plaintext HMAC secret. Shown once; deliver to the merchant securely.
                    pattern: "^[0-9a-f]{64}$"
        "400":
          description: Merchant has no `webhookUrl` — nothing to sign for
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }

  /admin/merchants/{id}/deactivate:
    post:
      tags: [admin]
      summary: Deactivate a merchant
      description: |
        Sets `active=false`. Inbound API requests with the merchant's API
        key are rejected with 401 while inactive. Idempotent. Deactivation
        does NOT invalidate the stored key hash — reactivation restores
        access with the same credential. Rotate the key first if deactivation
        is prompted by a credential leak.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Merchant deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      active: { type: boolean }
                      updatedAt: { type: string, format: date-time }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }

  /admin/merchants/{id}/activate:
    post:
      tags: [admin]
      summary: Reactivate a merchant
      description: Sets `active=true`. Idempotent.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Merchant activated
          content:
            application/json:
              schema:
                type: object
                properties:
                  merchant:
                    type: object
                    properties:
                      id: { type: string, format: uuid }
                      name: { type: string }
                      active: { type: boolean }
                      updatedAt: { type: string, format: date-time }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Merchant not found }

  /admin/bootstrap/alchemy-webhooks:
    post:
      tags: [admin]
      summary: Idempotently create Alchemy webhooks for the configured chains
      description: |
        For each requested chainId, checks whether an Alchemy ADDRESS_ACTIVITY
        webhook already exists with the target URL + network; creates it if
        not. Returns per-chain status plus the `signingKey` for any newly
        created webhook. Signing keys are persisted encrypted into
        `alchemy_webhook_registry` automatically; the `signingKey` field in
        the response is a one-shot safety net in case persistence raced.
        Requires `ALCHEMY_NOTIFY_TOKEN` env.
      security:
        - AdminKey: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                chainIds:
                  type: array
                  items: { type: integer }
                  description: Defaults to `ALCHEMY_CHAINS` env, then the mainnet set
                seedAddressByChainId:
                  type: object
                  additionalProperties: { type: string }
                  description: Per-chain placeholder address (Alchemy requires ≥1 at create). Defaults to zero-address.
              description: |
                The target webhook URL is NOT accepted in the body. It is sourced from
                `GATEWAY_PUBLIC_URL` env (base origin) plus the canonical path
                `/webhooks/alchemy`. Env-only so a leaked ADMIN_KEY cannot be
                used to redirect Alchemy's traffic to an arbitrary host.
      responses:
        "200":
          description: Bootstrap complete; per-chain status in body
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        chainId: { type: integer }
                        status:
                          type: string
                          enum: [created, existing, unsupported, failed]
                        webhookId: { type: string }
                        signingKey:
                          type: string
                          description: Only present when `status=created`. Automatically persisted to `alchemy_webhook_registry`; kept in the response as a one-shot safety net if `persisted=false`.
                        error: { type: string }
        "400":
          description: Missing ALCHEMY_NOTIFY_TOKEN or GATEWAY_PUBLIC_URL env
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }

  /admin/chains:
    get:
      tags: [admin]
      summary: Discovery — chains the gateway recognises and their bootstrap state
      description: |
        Read of the static chain registry, augmented with three readiness
        booleans per chain so the dashboard can decide what still needs
        bootstrapping in a single call:

        - `wired`             — a `ChainAdapter` for the chainId is loaded in `deps.chains`
        - `webhooksSupported` — chain *can* use Alchemy webhooks (EVM + Solana
          chains Alchemy serves). False for Tron — it's RPC-poll only and has
          no webhook concept. **Frontend should hide the "register webhook" CTA
          when this is false.**
        - `alchemyConfigured` — operator's `ALCHEMY_CHAINS` env actually
          includes this chainId. Independent of whether a webhook is registered.
        - `webhooks`          — a row exists in `alchemy_webhook_registry`.
          Only meaningful when `webhooksSupported=true`; false-and-irrelevant
          for RPC-poll chains.
        - `feeWallets`        — at least one **active** fee wallet exists.
        - `detection`         — `"alchemy"` when `webhooksSupported=true`, else
          `"rpc-poll"`. Pure derived field surfaced so the frontend doesn't
          have to know our chain-family taxonomy.
        - `bootstrapReady`    — convenience: chain is fully provisioned for
          payments + payouts. Formula adjusts for detection strategy:
          `wired && (!webhooksSupported || webhooks) && feeWallets`. So Tron
          can be ready with just `wired+feeWallets`, while EVM/Solana also need
          `webhooks`.

        The synthetic dev chain (chainId 999, slug `dev`) is **excluded** from
        this response — it's a test-harness chain only and operators should
        never attempt to register Alchemy webhooks or fee wallets for it.

        Tokens come from the static `TOKEN_REGISTRY` — they are the same for
        every operator and never include any per-merchant overrides. The
        endpoint never returns secrets (signing keys, seeds, env values).
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: family
          description: Narrow to one family.
          schema: { type: string, enum: [evm, tron, solana] }
        - in: query
          name: wired
          description: |
            `true` returns only chains with a loaded adapter; `false` returns
            registry-only entries; omit for both.
          schema: { type: string, enum: [true, false] }
      responses:
        "200":
          description: Chain inventory
          content:
            application/json:
              schema:
                type: object
                required: [chains]
                properties:
                  chains:
                    type: array
                    items:
                      type: object
                      required: [chainId, slug, family, displayName, wired, webhooksSupported, alchemyConfigured, webhooks, feeWallets, detection, bootstrapReady, confirmationsRequired, tokens]
                      properties:
                        chainId: { type: integer }
                        slug: { type: string, description: "Stable short id, e.g. 'ethereum' / 'tron-nile'." }
                        family: { type: string, enum: [evm, tron, solana] }
                        displayName: { type: string }
                        wired: { type: boolean, description: "True when a ChainAdapter for this chainId is loaded." }
                        webhooksSupported: { type: boolean, description: "True when this chain can use Alchemy Notify webhooks (EVM + Solana). False for RPC-poll chains like Tron — frontend should hide webhook CTAs." }
                        alchemyConfigured: { type: boolean, description: "True when operator's ALCHEMY_CHAINS env includes this chainId." }
                        webhooks: { type: boolean, description: "True when a row exists in alchemy_webhook_registry. Only meaningful if webhooksSupported is true." }
                        feeWallets: { type: boolean, description: "True when at least one active fee wallet exists for this chainId." }
                        detection: { type: string, enum: [alchemy, rpc-poll], description: "Which detection strategy this chain uses. Pure derived field for frontend convenience." }
                        bootstrapReady: { type: boolean, description: "wired && (!webhooksSupported || webhooks) && feeWallets — webhook requirement is dropped for RPC-poll chains." }
                        confirmationsRequired:
                          type: integer
                          description: "Confirmation threshold from `deps.confirmationThresholds`; transactions promote to `confirmed` at this count."
                        tokens:
                          type: array
                          items:
                            type: object
                            required: [symbol, decimals, isStable, displayName]
                            properties:
                              symbol: { type: string }
                              decimals: { type: integer }
                              isStable: { type: boolean }
                              displayName: { type: string }
                              contractAddress:
                                type: string
                                nullable: true
                                description: "Token contract / SPL mint address. Null for native assets (SOL, DEV)."
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }

  /admin/alchemy-webhooks:
    get:
      tags: [admin]
      summary: List Alchemy webhook registrations (one per chain)
      description: |
        Read-only view of `alchemy_webhook_registry`. The dashboard uses this
        to decide which chains are already wired versus which still need
        bootstrapping via `POST /admin/bootstrap/alchemy-webhooks`.

        The signing key is **never** returned (it's encrypted at rest via
        `SECRETS_ENCRYPTION_KEY`). Operators who need to verify a key should
        rotate it with `POST /admin/alchemy-webhooks/signing-keys`.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: chainId
          description: Narrow to a single chain.
          schema: { type: integer }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Paginated list of registered webhooks
          content:
            application/json:
              schema:
                type: object
                required: [webhooks, limit, offset, hasMore]
                properties:
                  webhooks:
                    type: array
                    items:
                      type: object
                      required: [chainId, webhookId, webhookUrl, createdAt, updatedAt]
                      properties:
                        chainId: { type: integer }
                        chain: { type: string, nullable: true, description: "Slug like 'ethereum' / 'polygon'; null when chainId is not in the static registry." }
                        webhookId: { type: string }
                        webhookUrl: { type: string, format: uri }
                        createdAt: { type: string, format: date-time }
                        updatedAt: { type: string, format: date-time }
                  limit: { type: integer }
                  offset: { type: integer }
                  hasMore: { type: boolean }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }

  /admin/alchemy-webhooks/signing-keys:
    post:
      tags: [admin]
      summary: Register an Alchemy webhook's signing key manually
      description: |
        For operators who created a webhook through the Alchemy dashboard UI
        (not via bootstrap) or who need to push a key back in after a
        persistence failure. The signing key is encrypted via
        `SECRETS_ENCRYPTION_KEY` before being written to
        `alchemy_webhook_registry`. Re-running with the same chainId upserts
        (rotation after a dashboard delete+recreate).
      security:
        - AdminKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chainId, webhookId, signingKey, webhookUrl]
              properties:
                chainId: { type: integer }
                webhookId: { type: string, minLength: 1, maxLength: 128 }
                signingKey: { type: string, minLength: 1, maxLength: 256 }
                webhookUrl: { type: string, format: uri }
      responses:
        "201":
          description: Signing key persisted encrypted-at-rest
          content:
            application/json:
              schema:
                type: object
                properties:
                  registered:
                    type: object
                    properties:
                      chainId: { type: integer }
                      webhookId: { type: string }
                      webhookUrl: { type: string, format: uri }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Admin surface not enabled (ADMIN_KEY not set) }

  /admin/fee-wallets:
    get:
      tags: [admin]
      summary: List fee wallets with reservation state
      description: |
        Operator view of the fee-wallet fleet. The `reservedByPayoutId` field
        surfaces the current CAS reservation (set while a payout holds the
        wallet; released on status transition). Use `?reserved=true` to list
        every in-flight reservation across every chain — the fastest way to
        spot payouts stuck in `reserved`/`submitted` status.

        **Native balance enrichment** — pass `?includeBalance=true` to add
        `nativeSymbol` / `nativeDecimals` / `nativeBalance` to every row. This
        costs **one RPC per row** (cap response with `limit` accordingly), so
        it's opt-in. Per-row failures are isolated: a single dead RPC sets
        `nativeBalance: null` and `nativeBalanceError: "rpc_error"` for that
        row only, the rest of the page still returns. Chains without a loaded
        adapter return `nativeBalanceError: "chain_not_wired"`.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: chainId
          schema: { type: integer, minimum: 1 }
        - in: query
          name: active
          schema: { type: string, enum: [true, false] }
        - in: query
          name: reserved
          schema: { type: string, enum: [true, false] }
        - in: query
          name: includeBalance
          description: |
            When `true`, fetch the native gas balance per row via one RPC each.
            Off by default to keep listing cheap.
          schema: { type: string, enum: [true, false] }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Fee-wallet page
          content:
            application/json:
              schema:
                type: object
                properties:
                  feeWallets:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: string }
                        chainId: { type: integer }
                        chain: { type: [string, "null"], description: "Stable slug like 'arbitrum'; null when chainId isn't in the static registry." }
                        address: { type: string }
                        label: { type: string }
                        active: { type: boolean }
                        reservedByPayoutId: { type: [string, "null"] }
                        reservedAt: { type: [string, "null"], format: date-time }
                        createdAt: { type: string, format: date-time }
                        nativeSymbol: { type: [string, "null"], description: "Only present when ?includeBalance=true. e.g. 'ETH', 'MATIC', 'TRX', 'SOL'." }
                        nativeDecimals: { type: [integer, "null"], description: "Only present when ?includeBalance=true. EVM=18, Tron=6, Solana=9." }
                        nativeBalance: { type: [string, "null"], description: "Only present when ?includeBalance=true. Raw integer string in smallest units (wei/sun/lamports). Divide by 10^nativeDecimals to display." }
                        nativeBalanceError: { type: [string, "null"], description: "Only present when ?includeBalance=true. Null on success; 'chain_not_wired' or 'rpc_error' on failure." }
                  limit: { type: integer }
                  offset: { type: integer }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [admin]
      summary: Register a fee wallet — address is HD-derived from MASTER_SEED
      description: |
        The gateway derives the fee-wallet's `(address, privateKey)` pair
        from `MASTER_SEED` at a deterministic index keyed by `(family, label)`.
        No private key is accepted over the admin API — fund the returned
        derived address out-of-band. The same label always derives to the
        same address across redeploys.
      security:
        - AdminKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chainId, label, family]
              properties:
                chainId: { type: integer }
                label:
                  type: string
                  maxLength: 64
                  description: Scope key (e.g. "hot-1", "cold-archive"). Hashed into a BIP32 index.
                family:
                  type: string
                  enum: [evm, tron, solana]
      responses:
        "201":
          description: Fee wallet registered; fund the returned address externally.
          content:
            application/json:
              schema:
                type: object
                properties:
                  feeWallet:
                    type: object
                    properties:
                      chainId: { type: integer }
                      address:
                        type: string
                        description: Derived address the operator must fund.
                      label: { type: string }
        "400":
          description: Family mismatch, MASTER_SEED missing, or validation error
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /admin/pool/initialize:
    post:
      tags: [admin]
      summary: Seed or top up the address pool per family
      description: |
        Idempotent. Families already at or above `initialSize` return as
        `already-sufficient` with zero additions. Families without a wired
        chain adapter in deps.chains return as `skipped-no-adapter`. Safe to
        call repeatedly and is the recovery path after a pool exhaustion.
      security:
        - AdminKey: []
      requestBody:
        required: false
        description: Empty body permitted; every field has a default.
        content:
          application/json:
            schema:
              type: object
              properties:
                families:
                  type: array
                  minItems: 1
                  items: { type: string, enum: [evm, tron, solana] }
                  default: [evm, tron, solana]
                initialSize:
                  type: integer
                  minimum: 1
                  maximum: 500
                  default: 5
      responses:
        "200":
          description: Initialization outcomes per family
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        family: { type: string, enum: [evm, tron, solana] }
                        status:
                          type: string
                          enum: [seeded, already-sufficient, skipped-no-adapter]
                        added: { type: integer }
        "400":
          description: Malformed JSON (BAD_JSON) or invalid families/initialSize
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /admin/pool/stats:
    get:
      tags: [admin]
      summary: Per-family pool utilization snapshot
      description: |
        Operators watch this endpoint + an alert on available-count under a
        threshold to catch "address supply running low" before POOL_EXHAUSTED
        fires on a live invoice-create.
      security:
        - AdminKey: []
      responses:
        "200":
          description: Pool statistics keyed by family
          content:
            application/json:
              schema:
                type: object
                properties:
                  stats:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        total: { type: integer }
                        available: { type: integer }
                        allocated: { type: integer }

  /admin/webhook-deliveries:
    get:
      tags: [admin]
      summary: List webhook deliveries by status
      description: |
        Operator view of the outbox. Use `status=dead` (default) to surface
        deliveries that exhausted the retry schedule and need manual
        intervention (merchant-side fix, then POST the replay endpoint).
        `pending` shows rows still queued for retry; `delivered` shows
        successful deliveries (mostly useful for audit).

        ### Outbound event types (`eventType` field)

        Invoice lifecycle (one row per status transition, idempotency key
        `{event}:{invoiceId}:{status}`):
          - `invoice.partial`, `invoice.detected`, `invoice.confirmed`,
            `invoice.overpaid`, `invoice.expired`, `invoice.canceled`
          - `invoice.demoted` — reorg un-confirmation; key includes
            `{previousStatus}:{newStatus}` instead of one status

        Per-transfer audit (one row per on-chain tx per stage; key
        `{event}:{invoiceId}:{txHash}` so detect→confirm produces TWO
        distinct rows for the same tx):
          - `invoice.transfer_detected` — first sighting of a pending
            transfer (push or poll). `data.payment.confirmations` is `0`
            for push-detected, `n` for poll-detected.
          - `invoice.payment_received` — same transfer reached the
            confirmation threshold.

        Payout lifecycle:
          - `payout.submitted`, `payout.confirmed`, `payout.failed`

        URL/secret resolution: per-resource override → merchant default
        → silently skipped (with a `warn` log so operators can detect
        misconfiguration). Body is signed with HMAC-SHA256 in
        `X-Webhook-Signature`; merchants should de-dup on
        `X-Webhook-Idempotency-Key`. Each row gets up to 5 outer retry
        rounds × 4 inline HTTP attempts before going `dead`.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: status
          schema: { type: string, enum: [pending, delivered, dead], default: dead }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 500, default: 50 }
        - in: query
          name: offset
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Page of delivery rows
          content:
            application/json:
              schema:
                type: object
                properties:
                  deliveries:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: string }
                        merchantId: { type: string }
                        eventType: { type: string }
                        status: { type: string, enum: [pending, delivered, dead] }
                        attempts: { type: integer }
                        lastStatusCode: { type: integer, nullable: true }
                        lastError: { type: string, nullable: true }
                        nextAttemptAt: { type: integer, nullable: true, description: "Epoch ms" }
                        deliveredAt: { type: integer, nullable: true, description: "Epoch ms" }
                        createdAt: { type: integer, description: "Epoch ms" }
                        updatedAt: { type: integer, description: "Epoch ms" }
        "400":
          description: Unknown status value (BAD_STATUS)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /admin/webhook-deliveries/{id}/replay:
    post:
      tags: [admin]
      summary: Replay a dead webhook delivery
      description: |
        Resets a delivery from `dead` back to `pending` with `next_attempt_at`
        set to now, so the next scheduled-jobs sweep re-drives it. Only rows
        in `dead` status are replayable — `pending` rows are already queued
        and `delivered` rows have already succeeded. Intended for when the
        merchant has fixed the underlying cause of permanent failure (TLS
        cert, URL change, etc.) and you want to push the event through.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Row reset to pending and queued for retry
          content:
            application/json:
              schema:
                type: object
                properties:
                  replayed:
                    type: object
                    properties:
                      id: { type: string }
                      nextAttemptAt: { type: integer, description: "Epoch ms" }
        "404":
          description: Delivery not found or not in dead status (NOT_DEAD)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /admin/orphan-transactions:
    get:
      tags: [admin]
      summary: List open orphan transactions
      description: |
        Orphans are detected on-chain transfers that could not be attributed
        to an active invoice at ingest time — typically payments on an
        address whose invoice has already expired / been canceled and whose
        pool cooldown window has elapsed.

        The list is backed by a partial index over `(chain_id, detected_at)`
        WHERE `invoice_id IS NULL AND dismissed_at IS NULL`, so the queue
        stays cheap even with millions of historical tx rows. Dismissed rows
        are hidden but remain in `transactions` for audit.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: chainId
          required: false
          schema: { type: integer, minimum: 1 }
        - in: query
          name: limit
          required: false
          schema: { type: integer, minimum: 1, maximum: 500, default: 50 }
        - in: query
          name: offset
          required: false
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200":
          description: Open orphan queue
          content:
            application/json:
              schema:
                type: object
                properties:
                  orphans:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: string }
                        chainId: { type: integer }
                        txHash: { type: string }
                        logIndex: { type: [integer, "null"] }
                        fromAddress: { type: string }
                        toAddress: { type: string }
                        token: { type: string }
                        amountRaw: { type: string }
                        amountUsd: { type: [string, "null"] }
                        usdRate: { type: [string, "null"] }
                        blockNumber: { type: [integer, "null"] }
                        confirmations: { type: integer }
                        status: { type: string, enum: [orphaned] }
                        detectedAt: { type: string, format: date-time }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /admin/orphan-transactions/{id}/attribute:
    post:
      tags: [admin]
      summary: Attribute an orphan transaction to an invoice
      description: |
        Re-points an orphan's `invoice_id` at the target invoice, promotes
        its tx status (`orphaned` → `detected`/`confirmed` based on
        confirmation count vs chain threshold), and runs
        `recomputeInvoiceFromTransactions` with `viaAdminOverride=true`.

        The override allows a terminal invoice (`expired` / `canceled`) to
        flip to `confirmed` or `overpaid` when the attributed tx clears the
        confirm bar. Partial credits against a terminal invoice stay parked
        — the merchant's prior decision stands if the payment doesn't cover
        the invoice.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invoiceId]
              properties:
                invoiceId: { type: string, format: uuid }
      responses:
        "200":
          description: Orphan attributed
          content:
            application/json:
              schema:
                type: object
                properties:
                  attribution:
                    type: object
                    properties:
                      txId: { type: string }
                      invoiceId: { type: string, format: uuid }
                      invoiceStatusBefore: { type: string }
                      invoiceStatusAfter: { type: string }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Transaction or invoice not found }
        "409": { description: Transaction is not an open orphan (already attributed or dismissed) }

  /admin/orphan-transactions/{id}/dismiss:
    post:
      tags: [admin]
      summary: Dismiss an orphan transaction
      description: |
        Marks an orphan as reviewed-and-ignored by stamping `dismissed_at`
        and `dismiss_reason`. The row stays in `transactions` for audit but
        drops out of the open-orphans queue via the partial index.
      security:
        - AdminKey: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, minLength: 1, maxLength: 512 }
      responses:
        "200":
          description: Orphan dismissed
          content:
            application/json:
              schema:
                type: object
                properties:
                  dismissal:
                    type: object
                    properties:
                      txId: { type: string }
                      dismissedAt: { type: string, format: date-time }
                      reason: { type: string }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Transaction not found }
        "409": { description: Transaction is attributed or already dismissed }

  /admin/audit-address:
    post:
      tags: [admin]
      summary: Audit an address by diffing on-chain history against stored transactions
      description: |
        Queries the chain adapter's `scanIncoming` for every registered token
        on `chainId` (plus the native symbol), diffs results against
        `transactions`, and ingests missing rows through the normal ingest
        path. Idempotent — already-stored txs are silently skipped via the
        UNIQUE `(chain_id, tx_hash, log_index)` constraint.

        Use when a merchant reports a missing payment: operator runs audit
        against the invoice's receive address, and any newly-discovered
        transfer either credits the invoice (if it's still active) or lands
        as an orphan for attribution / dismissal.

        `sinceMs` defaults to 30 days before the current server time; the
        adapter's own max-scan window still clamps the final range.
      security:
        - AdminKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chainId, address]
              properties:
                chainId: { type: integer, minimum: 1 }
                address: { type: string, minLength: 1, maxLength: 128 }
                sinceMs: { type: integer, minimum: 0, description: "Epoch ms lower bound; defaults to now - 30 days" }
      responses:
        "200":
          description: Audit complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  audit:
                    type: object
                    properties:
                      chainId: { type: integer }
                      address: { type: string }
                      sinceMs: { type: integer }
                      scanned: { type: integer, description: "Total transfers returned by the chain adapter" }
                      inserted: { type: integer, description: "Newly-added tx rows" }
                      alreadyPresent: { type: integer, description: "Rows skipped (already in transactions)" }
                      insertedTxIds:
                        type: array
                        items: { type: string }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /admin/balances:
    get:
      tags: [admin]
      summary: Snapshot gateway-owned address balances (DB-derived by default)
      description: |
        Returns per-address + per-chain + per-family token balances with USD
        totals, shaped as:

          token → address → chain → family → grand total

        Each level carries its own `totalUsd`. The top-level `source` field
        tells the caller which math produced the numbers: `"db"` or `"rpc"`.

        ### Mode — `source="db"` (default, fast)

        Computes balances from recorded activity only — no RPC traffic:

          * pool address balance = SUM(`transactions.amountRaw` WHERE
            `toAddress = addr` AND `status = 'confirmed'`)
          * fee wallet balance = SUM(confirmed transactions in) −
            SUM(confirmed `payouts.amountRaw` FROM `sourceAddress = addr`),
            clamped at 0.

        Returns in ~50ms regardless of pool size. Addresses with no
        recorded activity are omitted. **Caveat:** off-system movements
        are invisible (external fee-wallet topups, dust sent to
        unsubscribed pool addresses). Use `?live=true` when reconciling.

        ### Mode — `source="rpc"` (opts-in via `?live=true`)

        Asks every wired chain adapter for the live on-chain balance of
        every (address, chainId) pair. Authoritative but expensive:
        typical 15–30s cold for a full mainnet pool. Use for post-topup
        reconciliation or audit; do not bind to dashboard auto-refresh.

        Pricing (both modes): USD rates are fetched once per snapshot via
        `priceOracle.getUsdRates([...symbols])`. Tokens the oracle can't
        price get `usdRate=null` and `usd="0.00"` so callers can
        distinguish "really zero" from "couldn't price".

        ### Caching

        Keyed by mode + query string (`family`, `chainId`, `kind`,
        `address`). DB mode uses a 5s TTL (it's cheap to recompute);
        RPC mode uses 60s (dashboard refresh would otherwise hammer the
        provider). `db` and `rpc` entries do not share a slot. The
        response includes `cached: true|false` so dashboards can show
        staleness.
      security:
        - AdminKey: []
      parameters:
        - in: query
          name: family
          schema: { type: string, enum: [evm, tron, solana] }
          description: Narrow to one family.
        - in: query
          name: chainId
          schema: { type: integer }
          description: Narrow to one chainId. Implicitly narrows family.
        - in: query
          name: kind
          schema: { type: string, enum: [pool, fee] }
          description: Narrow to one address kind. Default returns both.
        - in: query
          name: address
          schema: { type: string }
          description: Spot-check one address without paying the full sweep cost.
        - in: query
          name: live
          schema: { type: string, enum: ["true", "1"] }
          description: |
            Flip to `true` or `1` to force a live RPC snapshot
            (`source="rpc"`). Omit for the fast DB-derived default.
      responses:
        "200":
          description: Snapshot computed (or served from cache)
          content:
            application/json:
              schema:
                type: object
                required: [snapshot, cached]
                properties:
                  snapshot: { $ref: "#/components/schemas/BalanceSnapshot" }
                  cached:
                    type: boolean
                    description: True when this response came from the in-memory cache instead of a fresh compute.
        "400":
          description: Invalid query parameter (BAD_FAMILY, BAD_CHAIN_ID, BAD_KIND)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /admin/migrate:
    post:
      tags: [admin]
      summary: Apply any pending schema migrations
      description: |
        Runs the bundled migration set through the idempotent migration-runner.
        Already-applied ids are skipped. On Workers/Vercel-Edge (no filesystem
        at runtime) this endpoint returns 501 — operators use `wrangler d1
        migrations apply` or the Turso migration CLI instead.
      security:
        - AdminKey: []
      responses:
        "200":
          description: Migration sweep result
          content:
            application/json:
              schema:
                type: object
                properties:
                  applied:
                    type: array
                    items: { type: string }
                  skipped:
                    type: array
                    items: { type: string }
        "501":
          description: No bundled migration set on this runtime (MIGRATIONS_NOT_BUNDLED)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /internal/cron/tick:
    post:
      tags: [internal]
      summary: Trigger the full scheduled-jobs sweep
      description: |
        Used by Vercel Cron / Upstash QStash / external schedulers. Returns
        per-job outcomes so a slow provider in one step doesn't mask failures
        elsewhere.
      security:
        - CronSecret: []
      responses:
        "200":
          description: Sweep completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  result:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        ok: { type: boolean }
                        error: { type: string }
        "401": { description: Missing or wrong Bearer token }
        "404": { description: Cron endpoint not enabled (CRON_SECRET not set) }

components:
  securitySchemes:
    MerchantApiKey:
      type: http
      scheme: bearer
      description: |
        Merchant API key returned once at `POST /admin/merchants`.
        Also accepted as `X-API-Key: <key>`.
    AdminKey:
      type: http
      scheme: bearer
      description: Shared admin key from `ADMIN_KEY` env. Also accepted as `X-Admin-Key`.
    AlchemySignature:
      type: apiKey
      in: header
      name: X-Alchemy-Signature
      description: HMAC-SHA256 hex of the raw body under the per-webhook signing key stored in `alchemy_webhook_registry` (looked up by `event.webhookId`).
    CronSecret:
      type: http
      scheme: bearer
      description: Shared secret from `CRON_SECRET` env.

  headers:
    X-Request-Id:
      description: UUID correlation id. Accepts a caller-supplied value if it's a valid UUID; otherwise minted.
      schema: { type: string, format: uuid }
    X-RateLimit-Limit:
      description: Window cap for this route's rate-limit scope
      schema: { type: integer }
    X-RateLimit-Remaining:
      description: Requests left in the current window after this one
      schema: { type: integer }
    X-RateLimit-Reset:
      description: Unix seconds when the window rolls
      schema: { type: integer }

  responses:
    Unauthorized:
      description: Missing or invalid API key
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    MerchantInactive:
      description: Merchant is deactivated
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    NotFound:
      description: Entity not found (or not owned by this merchant)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    ValidationError:
      description: Request body failed schema validation
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    RateLimited:
      description: Too many requests in the current window
      headers:
        Retry-After:
          description: Seconds until the window rolls
          schema: { type: integer }
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }

  schemas:
    Merchant:
      type: object
      description: |
        Merchant row as returned by the admin GET endpoints. Secret-adjacent
        fields (`api_key_hash`, `webhook_secret_ciphertext`) are never
        included.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        webhookUrl: { type: [string, "null"] }
        active: { type: boolean }
        paymentToleranceUnderBps: { type: integer, minimum: 0, maximum: 2000 }
        paymentToleranceOverBps: { type: integer, minimum: 0, maximum: 2000 }
        addressCooldownSeconds: { type: integer, minimum: 0, maximum: 604800 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code]
          properties:
            code: { type: string, description: Stable machine-readable code }
            message: { type: string }
            details:
              type: object
              additionalProperties: true
              description: Optional per-code payload (e.g. Zod issues on VALIDATION)

    CreateInvoiceInput:
      type: object
      description: |
        Provide EXACTLY ONE pricing mode:
          - `amountUsd` — USD-pegged. Pair with `acceptedFamilies` to make the
            invoice payable from any family/token the gateway supports. Rates
            are pinned for 10 minutes at creation.
          - `amountRaw` — legacy single-token. Payer must pay in `token` on
            `chainId`, exact amount.
          - `fiatAmount` + `fiatCurrency` — legacy fiat-quoted. Converted to
            a token amount at creation via the price oracle.
      properties:
        chainId: { type: integer }
        token: { type: string, pattern: "^[A-Z0-9]+$" }
        amountUsd:
          type: string
          pattern: "^\\d+(\\.\\d{1,8})?$"
          description: |
            USD-pegged target. Combined with `acceptedFamilies` lets the same
            invoice be settled in USDC, USDT, ETH, SOL, TRX, etc. across every
            wired chain.
        acceptedFamilies:
          type: array
          items: { type: string, enum: [evm, tron, solana] }
          minItems: 1
          description: |
            Families the invoice will accept payment from. Each family gets one
            allocated receive address. Omit for single-family (derived from
            `chainId`'s family).
        amountRaw:
          type: string
          pattern: "^\\d+$"
          description: Raw token amount in smallest units.
        fiatAmount:
          type: string
          pattern: "^\\d+(\\.\\d+)?$"
        fiatCurrency:
          type: string
          pattern: "^[A-Z]{3}$"
        externalId:
          type: string
          maxLength: 256
          description: |
            Merchant's opaque reference (e.g. cart id). Doubles as a Stripe-
            style idempotency key: a second `POST /api/v1/invoices` from the
            same merchant with the same `externalId` returns the original
            invoice (HTTP 201 again, identical body) instead of creating a
            duplicate. Safe to retry on network errors. Omit it and every
            POST creates a new invoice.
        metadata:
          type: object
          additionalProperties: true
        expiresInMinutes:
          type: integer
          minimum: 1
          maximum: 1440
          default: 30
        webhookUrl:
          type: string
          format: uri
          description: |
            Per-invoice webhook destination override. When set, dispatch for
            this invoice's events goes here instead of the merchant-account
            webhook. Must be supplied together with `webhookSecret` — passing
            one without the other is a 400. Echoed in responses; the secret is
            never returned.
        webhookSecret:
          type: string
          minLength: 16
          maxLength: 512
          writeOnly: true
          description: |
            HMAC secret paired with `webhookUrl`. Encrypted at rest and never
            echoed back. Required if `webhookUrl` is set.
        paymentToleranceUnderBps:
          type: integer
          minimum: 0
          maximum: 2000
          description: |
            Per-invoice under-payment tolerance override in basis points. Omit
            to inherit the merchant default (snapshotted at create time). See
            `POST /admin/merchants` for the math semantics.
        paymentToleranceOverBps:
          type: integer
          minimum: 0
          maximum: 2000
          description: |
            Per-invoice over-payment tolerance override in basis points. Omit
            to inherit the merchant default.
      required: [chainId, token]

    Invoice:
      type: object
      properties:
        id: { type: string, format: uuid }
        merchantId: { type: string, format: uuid }
        status:
          type: string
          enum: [created, partial, detected, confirmed, overpaid, expired, canceled]
        chainId: { type: integer }
        token: { type: string }
        receiveAddress: { type: string }
        addressIndex: { type: integer }
        acceptedFamilies:
          type: array
          items: { type: string, enum: [evm, tron, solana] }
        receiveAddresses:
          type: array
          description: One entry per accepted family. The checkout UI renders all of them.
          items:
            type: object
            properties:
              family: { type: string, enum: [evm, tron, solana] }
              address: { type: string }
        requiredAmountRaw: { type: string }
        receivedAmountRaw: { type: string }
        fiatAmount: { type: [string, "null"] }
        fiatCurrency: { type: [string, "null"] }
        quotedRate: { type: [string, "null"] }
        amountUsd:
          type: [string, "null"]
          description: USD target. Non-null only on USD-path invoices.
        paidUsd:
          type: [string, "null"]
          description: Sum of confirmed transactions' USD value. Non-null on USD-path invoices.
        overpaidUsd:
          type: [string, "null"]
          description: max(paidUsd - amountUsd, 0). Non-null on USD-path invoices.
        rates:
          type: [object, "null"]
          additionalProperties: { type: string }
          description: |
            Pinned USD-per-token rates for this invoice's window (USD-path only).
            Keys are token symbols, values are decimal strings.
        rateWindowExpiresAt:
          type: [string, "null"]
          format: date-time
        externalId: { type: [string, "null"] }
        metadata: { type: [object, "null"] }
        webhookUrl:
          type: [string, "null"]
          format: uri
          description: |
            Per-invoice webhook URL override (echo-only). Null when the
            invoice falls back to the merchant-account webhook. The matching
            secret is write-only and never returned.
        paymentToleranceUnderBps:
          type: integer
          minimum: 0
          maximum: 2000
          description: |
            Effective under-payment tolerance for this invoice in basis points
            (snapshot of merchant default or per-invoice override at create time).
        paymentToleranceOverBps:
          type: integer
          minimum: 0
          maximum: 2000
          description: |
            Effective over-payment tolerance for this invoice in basis points
            (snapshot of merchant default or per-invoice override at create time).
        createdAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
        confirmedAt: { type: [string, "null"], format: date-time }
        updatedAt: { type: string, format: date-time }

    InvoiceAmountsBreakdown:
      type: object
      description: |
        USD-axis breakdown of an invoice's progress. All fields null on legacy
        single-token invoices (use `requiredAmountRaw` / `receivedAmountRaw`
        on the invoice itself for those).
      properties:
        requiredUsd: { type: [string, "null"] }
        confirmedUsd:
          type: [string, "null"]
          description: Sum of CONFIRMED contributing transactions' USD values.
        confirmingUsd:
          type: [string, "null"]
          description: Sum of DETECTED-but-not-yet-confirmed transactions' USD values.
        remainingUsd:
          type: [string, "null"]
          description: max(requiredUsd - confirmedUsd, 0).
        overpaidUsd:
          type: [string, "null"]
          description: max(confirmedUsd - requiredUsd, 0).

    InvoiceTransaction:
      type: object
      description: |
        One on-chain payment tied to the invoice. All statuses (`detected`,
        `confirmed`, `reverted`, `orphaned`) are returned so debugging can
        see rows that no longer count toward the invoice total.
      properties:
        id: { type: string, format: uuid }
        txHash: { type: string }
        logIndex: { type: [integer, "null"] }
        chainId: { type: integer }
        chain:
          type: [string, "null"]
          description: Short slug like `ethereum`, `polygon`, `tron`, `solana`. Null if chainId is unknown to the registry.
        token: { type: string }
        fromAddress: { type: string }
        toAddress: { type: string }
        amountRaw:
          type: string
          description: Raw integer string in the token's smallest units.
        amount:
          type: string
          description: Human-readable decimal (e.g. `"1"`, `"0.04"`). Convenience for UIs.
        amountUsd:
          type: [string, "null"]
          description: USD valuation pinned at detection time.
        usdRate: { type: [string, "null"] }
        status:
          type: string
          enum: [detected, confirmed, reverted, orphaned]
        confirmations: { type: integer }
        blockNumber: { type: [integer, "null"] }
        detectedAt: { type: string, format: date-time }
        confirmedAt: { type: [string, "null"], format: date-time }

    InvoiceDetails:
      type: object
      description: Hydrated GET response — invoice plus USD breakdown plus per-tx detail.
      required: [invoice, amounts, transactions]
      properties:
        invoice: { $ref: "#/components/schemas/Invoice" }
        amounts: { $ref: "#/components/schemas/InvoiceAmountsBreakdown" }
        transactions:
          type: array
          items: { $ref: "#/components/schemas/InvoiceTransaction" }

    PublicInvoice:
      type: object
      description: Same as Invoice but with merchant-private fields stripped
      properties:
        id: { type: string, format: uuid }
        status: { type: string }
        chainId: { type: integer }
        token: { type: string }
        receiveAddress: { type: string }
        requiredAmountRaw: { type: string }
        receivedAmountRaw: { type: string }
        fiatAmount: { type: [string, "null"] }
        fiatCurrency: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time }
        confirmedAt: { type: [string, "null"], format: date-time }

    PlanPayoutInput:
      type: object
      required: [chainId, token, amountRaw, destinationAddress]
      properties:
        chainId: { type: integer }
        token: { type: string }
        amountRaw: { type: string, pattern: "^\\d+$" }
        destinationAddress: { type: string, minLength: 1, maxLength: 128 }
        webhookUrl:
          type: string
          format: uri
          description: |
            Per-payout webhook destination override. When set, dispatch for
            this payout's events goes here instead of the merchant-account
            webhook. Must be supplied together with `webhookSecret` — passing
            one without the other is a 400. Echoed in responses; the secret is
            never returned.
        webhookSecret:
          type: string
          minLength: 16
          maxLength: 512
          writeOnly: true
          description: |
            HMAC secret paired with `webhookUrl`. Encrypted at rest and never
            echoed back. Required if `webhookUrl` is set.

    BalanceSnapshot:
      type: object
      required: [generatedAt, source, totalUsd, families]
      properties:
        generatedAt: { type: string, format: date-time }
        source:
          type: string
          enum: [db, rpc]
          description: |
            Where the numbers came from. `db` = derived from recorded
            transactions/payouts (fast default). `rpc` = live chain
            snapshot (opts-in via `?live=true`).
        totalUsd:
          type: string
          description: Grand total USD across every family in the snapshot, fixed to 2 decimal places.
        families:
          type: array
          items: { $ref: "#/components/schemas/FamilyBalance" }

    FamilyBalance:
      type: object
      required: [family, totalUsd, chains]
      properties:
        family: { type: string, enum: [evm, tron, solana] }
        totalUsd: { type: string }
        chains:
          type: array
          items: { $ref: "#/components/schemas/ChainBalance" }

    ChainBalance:
      type: object
      required: [chainId, totalUsd, tokens, addresses, errors]
      properties:
        chainId: { type: integer }
        totalUsd: { type: string }
        tokens:
          type: array
          description: Per-token roll-up across every address on this chain.
          items:
            type: object
            required: [token, amountRaw, amountDecimal, usd]
            properties:
              token: { type: string }
              amountRaw: { type: string, description: Sum of raw token amounts (smallest units). }
              amountDecimal: { type: string }
              usd: { type: string }
        addresses:
          type: array
          items: { $ref: "#/components/schemas/AddressBalance" }
        errors:
          type: integer
          description: |
            Number of addresses on this chain we tried to snapshot but couldn't
            (RPC error, missing adapter). Non-zero means the totals are partial
            and shouldn't be read as authoritative.

    AddressBalance:
      type: object
      required: [address, kind, totalUsd, tokens]
      properties:
        address: { type: string }
        kind: { type: string, enum: [pool, fee] }
        poolStatus:
          type: string
          enum: [available, allocated, quarantined]
          description: Present only on `kind=pool` rows.
        poolAllocatedToInvoiceId:
          type: [string, "null"]
          description: |
            Invoice this pool address is currently allocated to (when
            `poolStatus=allocated`). Present only on `kind=pool` rows.
        feeLabel:
          type: string
          description: Operator label of the fee wallet (e.g. `hot-1`). Present only on `kind=fee` rows.
        totalUsd: { type: string }
        tokens:
          type: array
          items: { $ref: "#/components/schemas/TokenBalance" }

    TokenBalance:
      type: object
      required: [token, decimals, amountRaw, amountDecimal, usd, usdRate]
      properties:
        token: { type: string }
        decimals: { type: integer }
        amountRaw:
          type: string
          description: Raw integer string in the token's smallest units.
        amountDecimal:
          type: string
          description: Human-readable decimal form (UI convenience).
        usd:
          type: string
          description: USD value of this balance. `"0.00"` when the token isn't priced (oracle miss); check `usdRate` to distinguish.
        usdRate:
          type: [string, "null"]
          description: USD-per-token rate at snapshot time. `null` when the price oracle had no entry.

    Payout:
      type: object
      properties:
        id: { type: string, format: uuid }
        merchantId: { type: string, format: uuid }
        status:
          type: string
          enum: [planned, reserved, submitted, confirmed, failed, canceled]
        chainId: { type: integer }
        token: { type: string }
        amountRaw: { type: string }
        destinationAddress: { type: string }
        sourceAddress: { type: [string, "null"] }
        txHash: { type: [string, "null"] }
        feeEstimateNative: { type: [string, "null"] }
        lastError: { type: [string, "null"] }
        webhookUrl:
          type: [string, "null"]
          format: uri
          description: |
            Per-payout webhook URL override (echo-only). Null when the payout
            falls back to the merchant-account webhook. The matching secret
            is write-only and never returned.
        createdAt: { type: string, format: date-time }
        submittedAt: { type: [string, "null"], format: date-time }
        confirmedAt: { type: [string, "null"], format: date-time }
        updatedAt: { type: string, format: date-time }
